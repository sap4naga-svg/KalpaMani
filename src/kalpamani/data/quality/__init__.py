"""Deterministic data-quality checks.

Same inputs, same findings. No sampling, no thresholds tuned by eye, no model
deciding what looks wrong. Findings are typed data so the code that refuses to
serve a result can query them.

Research, strategy, risk and portfolio code may not import this package.
"""
