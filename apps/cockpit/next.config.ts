import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The development indicator overlays the navigation rail and would appear in every
  // review screenshot. It is a development affordance, not part of the interface.
  devIndicators: false,
};

export default nextConfig;
